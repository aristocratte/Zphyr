# Zphyr Evaluation Harness — Python Dependencies
#
# Core (always required, no model downloads):
#   jiwer      — WER/CER computation
#   rich       — terminal output formatting
#   tabulate   — table formatting for reports
#
# Install: pip install -r requirements.txt
#
# Optional semantic scoring (--semantic flag only):
#   sentence-transformers  — MiniLM-L6 cosine similarity (~90MB one-time download)
#   numpy                  — required by sentence-transformers
#
# Install optional: pip install sentence-transformers numpy

jiwer>=3.0
rich>=13.0
tabulate>=0.9
